# TODO — 미해결 과제

## 수정 계약서 다운로드 — 한글(HWP) 및 한국어 인코딩 이슈

**상태**: 미해결
**관련 파일**: `app/api/v1/reports.py`, `_generate_docx()`, `_generate_pdf()`

### 문제
- python-docx로 DOCX 생성 시 한국어 텍스트가 깨지는 현상 발생
- 원본이 HWP/HWPX 파일인 경우 원본 포맷 그대로 수정본을 생성할 수 없음
- pymupdf PDF 생성 시 한글 폰트 임베딩 문제 가능성

### 해결 방향 (검토 필요)
1. **DOCX 한글 깨짐**: 폰트 임베딩 확인, `python-docx`에서 `맑은 고딕` 폰트가 실제로 적용되는지 검증. 서버 환경(Linux)에 한글 폰트 설치 필요할 수 있음
2. **PDF 한글**: pymupdf `insert_text()`에 한글 폰트 경로를 명시적으로 지정하거나, reportlab + 한글 폰트 조합으로 교체 검토
3. **HWP 수정본 생성**: python-hwp 또는 pyhwpx 라이브러리 검토, 또는 LibreOffice CLI를 통한 변환 파이프라인 구축
4. **대안**: 웹에서 수정 전/후 diff를 보여주고, 사용자가 직접 복사-붙여넣기하는 UX도 고려

### 참고
- 현재 업로드 시 원본 파일 바이트를 저장하지 않음 (raw_text만 DB 저장)
- 원본 포맷 유지가 필요하면 업로드 시 파일 자체도 저장하는 구조 변경 필요
